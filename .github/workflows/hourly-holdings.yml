name: Hourly holdings capture

on:
  schedule:
    - cron: "15 * * * *"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: holdings-capture
  cancel-in-progress: false

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: python -m pytest
      - name: Fetch holdings CSVs
        run: python -m scripts.fetch_holdings
      - name: Capture NAV observations
        run: python -m scripts.fetch_nav
      - name: Capture market prices
        run: python -m scripts.fetch_market_prices
      - name: Build site data
        run: python -m scripts.build_site_data
      - name: Commit captured data
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data site
          git commit -m "Capture latest EasyETFs holdings" || echo "No changes to commit"
          git pull --rebase || true
          git push
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: capture
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
